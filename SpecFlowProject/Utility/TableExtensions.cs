using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection.Metadata.Ecma335;
using System.Text;
using System.Threading.Tasks;
using System.Collections.Generic;
using TechTalk.SpecFlow;
using AventStack.ExtentReports.Model;
using NUnit.Framework;
namespace StoreAdmin_BDD.Utility
{
    public class TableExtensions
    {
        /*
        public static Dictionary<string, string> ToDictionary(Table table)
        {

            var dictionary = new Dictionary<string, string>();
            foreach (var row in table.Rows)
            {
                dictionary.Add(row[0], row[1]);
            }
            return dictionary;

        }

        */
    }
}







































/*
public class YourSteps
{
    [When(@"User enter credentials")]
    public void WhenUserEnterCredentials(Table table)
    {
        Dictionary<string, string> credentials = table.ToDictionary();

        // Now you can access the values from the dictionary and perform your actions
        string awardCategory    = credentials["Award Category"];
        string awardType        = credentials["Award Type"];
        string chooseDate       = credentials["Choose a date"];

        // Implement the logic to use these values
    }


*/
